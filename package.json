{
  "name": "project-aegis",
  "version": "1.0.0",
  "scripts": {
    "start": "DB_PATH=${DB_PATH:-/tmp/aegis_enterprise.db} node backend/enterprise-server.js",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install",
    "postinstall": "npm rebuild sqlite3 --build-from-source"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "sqlite3": "^5.1.7"
  }
}
